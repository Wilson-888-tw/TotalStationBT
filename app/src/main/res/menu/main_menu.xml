<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- 工具列常駐按鈕 -->
    <item
        android:id="@+id/menu_map"
        android:title="2D 平面圖"
        android:icon="@drawable/ic_map_plan"
        app:showAsAction="always" />
    <item
        android:id="@+id/menu_connect"
        android:title="藍牙連線"
        android:icon="@drawable/ic_bluetooth_search"
        app:showAsAction="always" />

    <!-- 專案管理 -->
    <group android:id="@+id/group_project">
        <item
            android:id="@+id/menu_projects"
            android:title="管理專案"
            android:orderInCategory="100"
            app:showAsAction="never" />
    </group>

    <!-- 匯出 / 匯入 -->
    <group android:id="@+id/group_export">
        <item
            android:id="@+id/menu_export_csv"
            android:title="匯出 CSV"
            android:orderInCategory="200"
            app:showAsAction="never" />
        <item
            android:id="@+id/menu_export_dxf"
            android:title="匯出 AutoCAD (.dxf)"
            android:orderInCategory="201"
            app:showAsAction="never" />
        <item
            android:id="@+id/menu_export_raw"
            android:title="匯出原始資料"
            android:orderInCategory="202"
            app:showAsAction="never" />
        <item
            android:id="@+id/menu_upload_nikon"
            android:title="整批上傳至 Nikon 儀器"
            android:orderInCategory="203"
            app:showAsAction="never" />
    </group>

    <!-- 資料管理 -->
    <group android:id="@+id/group_manage">
        <item
            android:id="@+id/menu_clear"
            android:title="清除資料"
            android:orderInCategory="300"
            app:showAsAction="never" />
    </group>

    <!-- 說明 -->
    <group android:id="@+id/group_info">
        <item
            android:id="@+id/menu_help"
            android:title="操作說明"
            android:orderInCategory="399"
            app:showAsAction="never" />
        <item
            android:id="@+id/menu_about"
            android:title="關於"
            android:orderInCategory="400"
            app:showAsAction="never" />
    </group>

</menu>
